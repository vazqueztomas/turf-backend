[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
passlib = "==1.7.4"
pypdf = "==5.1.0"
sqlmodel = "==0.0.22"
psycopg2-binary = "==2.9.10"
fastapi = {extras = ["standard"] }
bcrypt = "==4.3.0"
pdfplumber = "*"
pydantic-settings = "==2.7.1"
requests = "==2.32.5"

[dev-packages]
ruff = "==0.9.1"
pylint = "==3.3.3"
pytest = "==8.3.3"
ipykernel = "*"
pytest-cov = "==6.0.0"
pytest-mock = "==3.14.0"
pre-commit = "==4.0.1"

[requires]
python_version = "3.12"

[scripts]
format = "pre-commit run --all-files"
test = "pytest --cov=turf_backend --cov-report term-missing:skip-covered --cov-report xml:coverage-report.xml --junitxml=coverage.xml tests"
server = "sh -c 'docker compose up -d && uvicorn turf_backend.main:app --reload'"
